<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.first.rcare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".workandlesiure"></activity>
        <activity android:name=".Attributemood" />
        <activity
            android:name=".Careset"
            android:label="@string/title_activity_careset"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HealthyDiscussion"
            android:label="@string/title_activity_healthy_discussion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Basicdata"
            android:label="@string/title_activity_basicdata"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rcare"
            android:label="@string/title_activity_rcare"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>